"""Development systems stories with explicit, pre-execution relevance judgments."""
from spec import C, Q, R

CASES = [
 C("ledger_refunds", "Ledger", "Tomas", "refunds", [
  R("window", "Card refund interval", "Ledger permits a card refund during the first thirty calendar days after capture. Authorization time is not the start of this interval."),
  R("clock", "Refund age calculation", "refundAgeDays in payments/refunds/age.py compares capture_date with the merchant's local business date. REFUND_TOO_OLD is raised after day thirty.", kind="code-reference"),
  R("partial", "Partial refund accounting", "Each partial refund subtracts from the captured balance. Multiple partial refunds are allowed while their total does not exceed the original capture.", kind="decision"),
  R("dispute", "Chargeback handling", "A chargeback freezes self-service refunds until the dispute task releases the payment. The operator can read the payment but cannot issue another partial refund."),
  R("old", "Former seven-day card rule", "The initial pilot allowed card refunds for only seven days from capture.", status="superseded", revision="r1"),
  R("change", "Thirty-day rollout", "The thirty-day card policy replaced the seven-day pilot after the merchant agreement changed. Store-credit returns were not part of this rollout.", kind="decision", supersedes=["old"], level=2),
  R("credit", "Store-credit expiry", "The store-credit task issues return vouchers that expire after ninety days. A voucher is not a reversal of the card capture.", task="store-credit"),
  R("other", "Ledger library returns", "The Library project calls its circulation log Ledger. Books may be returned after fourteen days without a late fee.", project="Library", owner="Ruth"),
  R("observation", "Partial refund replay", "A synthetic capture of forty units accepted refunds of ten and twenty. An additional fifteen-unit refund was refused because only ten remained.", kind="observation", epistemic="verified"),
  R("hypothesis", "Holiday extension idea", "Tomas suggested excluding public holidays when counting refund age. The proposal has no accepted policy or calendar implementation.", kind="hypothesis", epistemic="claimed"),
 ], [
  Q("code", "code-references", "Which routine produces REFUND_TOO_OLD?", {"clock":3}, "The code reference names both the routine and exact error identifier."),
  Q("paraphrase", "paraphrases", "Does the return clock begin when the bank approves payment or when funds are captured?", {"window":3,"clock":2}, "The interval rule explicitly starts at capture; the calculation record corroborates the date source."),
  Q("multi", "multi-record", "What stops an otherwise timely second partial refund?", {"partial":3,"dispute":3,"observation":1}, "Remaining captured balance and a chargeback freeze are independent blockers; the replay illustrates only the balance blocker."),
  Q("history", "historical-revisions", "How long was the refund window in the pilot?", {"old":3}, "The question targets the superseded r1 interval.", statuses=["superseded"], revisions=["r1"]),
  Q("scope", "same-name-entities", "How long before Ledger's return voucher expires?", {"credit":3}, "Return voucher denotes the store-credit task, not card refunds or the Library log.", tasks=["store-credit"]),
  Q("unapproved", "unsupported-hypotheses", "Which approved holiday calendar extends the card refund interval?", {}, "Only an unsupported suggestion mentions holidays; no approved calendar is present."),
 ]),
 C("harbor_retries", "Harbor", "Sana", "webhook-delivery", [
  R("backoff", "Webhook retry schedule", "Harbor retries temporary webhook failures after two, ten, and forty minutes. A successful acknowledgment ends the schedule; the receiver must answer with a 2xx status."),
  R("symbol", "Delivery attempt ledger", "appendDeliveryAttempt in delivery/attempt_log.ts writes the destination, attempt number, and response class. It does not store the receiver's response body.", kind="code-reference"),
  R("permanent", "Permanent delivery failures", "HTTP 410 closes a webhook subscription immediately. Other 4xx responses enter the rejected queue for operator review; they do not follow the temporary retry schedule.", kind="decision"),
  R("journal", "Callback relay diagnostic journal", "The Harbor relay receives events from several business tasks and serializes each delivery into a small envelope. A run begins by recording a destination and an opaque event key; payload construction happens before the transport loop. The operator view groups attempts by subscription rather than by the producer's internal task name.\n\nDuring the first synthetic drill, a receiver alternated between successful acknowledgments and temporary server failures. The attempt ledger preserved the response class, and the dashboard showed the delay until the next scheduled attempt. These observations helped explain queue occupancy but did not establish a policy for permanently rejected destinations. A second drill removed a subscription after all its pending events were drained.\n\nSeveral earlier notes focused on dashboard timestamps, log colors, and the order in which rows appeared. None of those details determines whether a resumed delivery repeats an already accepted event. The response body was deliberately omitted from the ledger, so diagnosis used the status class and stable envelope key instead. The journal also records that destination edits create a new subscription version.\n\nThe decisive recovery rule is at the end of the drill: after an acknowledgment has been durably recorded, restarting the relay must not resend that event. The recovery cursor advances from the acknowledged attempt, not from the last event fetched from the producer.", kind="observation", epistemic="verified", level=2),
  R("old", "Pilot retry spacing", "Harbor's pilot repeated temporary failures every minute for three attempts.", status="superseded", revision="r1"),
  R("replacement", "Backoff change rationale", "The two-ten-forty-minute schedule replaced the one-minute pilot to reduce pressure on recovering receivers.", kind="decision", supersedes=["old"]),
  R("other", "Harbor import retry policy", "The import task retries a failed CSV parse only after the operator changes the input. It never waits on a timer.", task="csv-import"),
  R("pref", "Sana's delivery log preference", "Sana wants destination and response class visible in the delivery log. She does not want response bodies copied into the operator panel.", kind="preference", level=2),
  R("claim", "Receiver body retention claim", "A draft runbook claims full receiver responses are retained for seven days, but its example attachment was lost and the draft has no approval.", epistemic="claimed", provenance="missing"),
  R("revoked", "Withdrawn retry-everything advice", "An old support note told operators to retry every response, including HTTP 410. That advice was withdrawn because it reactivated removed subscriptions.", status="revoked", revision="r0"),
 ], [
  Q("schedule", "identifiers", "What happens to a Harbor subscription after HTTP 410?", {"permanent":3}, "The permanent-failure decision explicitly closes it; the revoked support note is ineligible."),
  Q("tail", "long-tail-evidence", "On restart, which saved event position keeps Harbor from repeating an accepted callback?", {"journal":3}, "Only the final paragraph states recovery must advance from the durably acknowledged attempt rather than the last fetched event."),
  Q("code", "code-references", "Where are delivery attempts recorded, and is the receiver body included?", {"symbol":3,"journal":2,"pref":1}, "The code reference names the writer and retained fields. The journal explicitly corroborates ledger-body omission; the preference supports omission in the UI but does not identify the code."),
  Q("multi", "multi-record", "Give the temporary retry delays and explain why they changed.", {"backoff":3,"replacement":3}, "Schedule and change rationale are separately requested."),
  Q("historical", "revoked-history", "What did the withdrawn support advice say about retrying removed subscriptions?", {"revoked":3}, "The question explicitly asks for withdrawn advice and therefore permits only the revoked r0 record.", statuses=["revoked"], revisions=["r0"]),
  Q("missing", "missing-evidence", "Which retained approved record proves webhook response bodies are kept for seven days?", {}, "The sole seven-day claim has missing evidence and is unapproved; retained records instead omit the body."),
 ]),
 C("atlas_paths", "Atlas", "Nico", "route-files", [
  R("root", "Route catalog root", "Atlas resolves route documents beneath catalog/routes. User-facing route names are not filesystem paths and may contain spaces."),
  R("symbol", "Canonical route path helper", "canonicalRoutePath in routing/path_policy.rs converts a route slug into a file beneath catalog/routes. A resolved path outside that root returns RoutePathOutsideCatalog.", kind="code-reference"),
  R("aliases", "Display-name aliases", "The route display name North Pass has aliases Northern Crossing and Ridge Link. All three refer to the same hiking route in Atlas's summer catalog."),
  R("winter", "North Pass winter route", "The winter-planning task uses North Pass for a snowshoe route that stops at the lower hut. It is not the summer hiking traverse.", task="winter-planning"),
  R("other", "North Pass transit stop", "Metro's North Pass stop is an underground platform on the copper line. It has no hiking route file.", project="Metro", owner="Ada"),
  R("old", "Flat route storage", "The first catalog placed route files directly in catalog, alongside image thumbnails.", status="superseded", revision="r1"),
  R("move", "Route directory migration", "Moving route documents into catalog/routes separated them from thumbnails. Existing display-name aliases remained valid after the migration.", kind="decision", supersedes=["old"], level=2),
  R("observation", "Alias lookup observation", "In the synthetic summer catalog, searching Ridge Link returned the North Pass hiking route and its route document.", kind="observation", epistemic="verified"),
  R("preference", "Nico's route labeling preference", "Nico prefers the canonical route name first, followed by known aliases in parentheses. File slugs should stay out of the public title.", kind="preference", level=2),
  R("lost", "Removed private route note", None, kind="note", status="deleted", epistemic="claimed", provenance=[]),
 ], [
  Q("error", "identifiers", "Which helper emits RoutePathOutsideCatalog?", {"symbol":3}, "Only the code reference binds the error to a helper and source path."),
  Q("alias", "same-name-entities", "Which summer hike is also called Ridge Link?", {"aliases":3,"observation":2}, "The alias declaration directly identifies it; the observation corroborates lookup. Winter and Metro names are outside scope."),
  Q("scope", "task-scope", "For winter planning, does North Pass reach the full hiking traverse?", {"winter":3}, "The winter-task record ends at the lower hut. Summer aliases must not supply this answer.", tasks=["winter-planning"]),
  Q("history", "supersession", "What changed in route-file storage when thumbnails were separated?", {"old":2,"move":3,"root":2}, "The migration explains the change and the old/current locations establish its endpoints.", statuses=["current","superseded"]),
  Q("multi", "multi-record", "Where are route files stored, and how should Nico display aliases?", {"root":3,"preference":3,"symbol":2,"move":2}, "Storage root and display preference are different requested facts. The helper and migration record explicitly corroborate the storage root."),
  Q("deleted", "deleted-payload", "What were the coordinates in the removed private route note?", {}, "The note's title survives but its payload does not; coordinates cannot be reconstructed from route aliases.", statuses=["current","deleted"]),
 ]),
 C("quill_preferences", "Quill", "Lea", "release-notes", [
  R("style", "Lea's release-note style", "Lea prefers release notes to lead with the user-visible result and then one concrete example. Internal ticket numbers belong in a final references line.", kind="preference", level=2),
  R("length", "Release note length decision", "Routine fixes get one paragraph in Quill release notes. Breaking changes may use a migration checklist because a paragraph can hide required user actions.", kind="decision"),
  R("code", "Release note formatter", "renderReleaseEntry in notes/render_entry.py keeps the result sentence before the example and renders references last. It accepts a migration_steps list only for breaking changes.", kind="code-reference"),
  R("old", "Former ticket-first style", "The earlier release-note template began each entry with the internal ticket number.", kind="preference", status="superseded", revision="r1"),
  R("change", "Readability revision", "Lea replaced ticket-first headings with result-first entries after external readers could not interpret ticket codes.", kind="decision", supersedes=["old"]),
  R("incident", "Incident report writing style", "For incident reports, Lea wants a timestamped timeline before the impact discussion. This ordering applies only to the incident-report task.", kind="preference", task="incident-report", level=2),
  R("other", "Lea at the Quill book club", "The Book Club's Lea prefers chapter summaries ordered by character. This is a different person in a different project.", project="Book Club", owner="Lea B"),
  R("observation", "Reader study observation", "Synthetic readers found the changed behavior in the first sentence of the new entries. The exercise did not measure whether migration checklists prevent mistakes.", kind="observation", epistemic="verified"),
  R("guess", "Emoji heading suggestion", "A writer suggested adding an emoji to every release-note heading. Lea has not expressed a preference about that suggestion.", kind="hypothesis", epistemic="claimed"),
  R("translation", "Translation handoff note", "Release-note examples should keep sample filenames unchanged during translation. Explanatory prose may be translated, but changing the filename breaks cross-language support references.", level=2),
 ], [
  Q("preference", "preferences", "How does Lea want routine release notes ordered?", {"style":3,"length":1,"code":2,"change":2,"observation":1}, "The preference directly gives ordering. The formatter and readability revision explicitly corroborate result-first entries; paragraph length and the reader observation supply limited context."),
  Q("paraphrase", "paraphrases", "When may a release entry be more than a paragraph?", {"length":3,"code":2}, "Breaking-change migration steps are the explicit exception; the formatter enforces the distinction."),
  Q("scope", "task-scope", "Should Lea put timestamps first in an incident write-up?", {"incident":3}, "This task-specific preference differs from release-note ordering.", tasks=["incident-report"]),
  Q("history", "historical-revisions", "Which element led Quill entries before the readability revision?", {"old":3}, "The superseded template begins with a ticket number.", statuses=["superseded"], revisions=["r1"]),
  Q("multi", "multi-record", "How are release references ordered, and what should translators preserve in examples?", {"style":3,"translation":3,"code":1}, "The preference and translation note supply distinct requirements; the formatter corroborates references-last behavior."),
  Q("unknown", "unsupported-hypotheses", "What emoji has Lea approved for every release heading?", {}, "A writer's suggestion is not an approved user preference, and no specific emoji is recorded."),
 ]),
 C("beacon_alerts", "Beacon", "Omar", "pager-routing", [
  R("threshold", "Queue age paging threshold", "Beacon pages the primary responder when the oldest queued job exceeds eight minutes for three consecutive samples. A single slow sample only adds a dashboard marker."),
  R("sampling", "Queue age sample interval", "The queue-age watcher samples every thirty seconds. Consecutive samples reset when the oldest job drops back to eight minutes or less."),
  R("symbol", "Pager state machine", "advanceAgeAlarm in monitor/age_alarm.go tracks consecutive over-limit samples. Its page transition emits QUEUE_AGE_PAGE with the responder rotation key.", kind="code-reference"),
  R("quiet", "Quiet-hours exception", "Queue-age pages are exempt from Beacon's quiet hours because delayed background dispatch can block customer work. Informational dashboard markers remain silent overnight.", kind="decision"),
  R("old", "Earlier single-sample paging", "Beacon previously paged on one sample above five minutes. Short dispatch pauses produced noisy pages.", status="superseded", revision="r1"),
  R("change", "Noise reduction decision", "The three-sample, eight-minute condition replaced single-sample paging. The thirty-second sampling cadence stayed unchanged.", kind="decision", supersedes=["old"], level=2),
  R("other", "Beacon battery warning", "The hardware-health task warns when a sensor battery falls below fifteen percent. It routes to a maintenance inbox, not the primary responder.", task="hardware-health"),
  R("observation", "Interrupted threshold run", "The synthetic sample series nine, nine, seven, nine minutes did not page. The seven-minute sample reset the consecutive counter.", kind="observation", epistemic="verified"),
  R("claim", "Unapproved percentile alarm", "Omar proposed paging on the ninety-fifth percentile job age, but the proposal has not replaced oldest-job sampling.", kind="hypothesis", epistemic="claimed"),
  R("missing", "Unretained escalation phone list", "A draft phone list claims to name the backup responder. Its source attachment is missing, so the list cannot establish the current backup number.", epistemic="claimed", provenance="missing"),
 ], [
  Q("identifier", "identifiers", "What condition emits QUEUE_AGE_PAGE?", {"threshold":3,"symbol":3,"sampling":2}, "Policy and state-machine records specify the page transition; cadence/reset context is useful but incomplete alone."),
  Q("paraphrase", "paraphrases", "Will one briefly old queued job wake the responder?", {"threshold":3,"observation":2,"change":2}, "A single sample only marks the dashboard. The interrupted run illustrates counter reset, and the revision corroborates the three-sample requirement."),
  Q("multi", "multi-record", "How often are old jobs checked, and are qualifying pages silenced overnight?", {"sampling":3,"quiet":3,"change":2}, "Sampling cadence and quiet-hours behavior are distinct required facts; the revision explicitly corroborates the unchanged thirty-second cadence."),
  Q("revision", "historical-revisions", "At revision r1, how many slow samples caused a page?", {"old":3}, "The r1 policy used one sample and is explicitly requested.", statuses=["superseded"], revisions=["r1"]),
  Q("hypothesis", "unsupported-hypotheses", "Which unapproved job-age statistic did Omar propose?", {"claim":3}, "This asks to retrieve the proposal as a proposal, not to treat it as current policy.", epistemics=["claimed"], require_evidence=False),
  Q("missing", "missing-evidence", "What is the evidenced current backup responder phone number?", {}, "The sole phone-list source is missing and supplies no verified current number."),
 ]),
 C("kiln_batching", "Kiln", "Pia", "ceramic-firing", [
  R("cone", "Stoneware firing target", "The Kiln stoneware program targets cone six. Earthenware bisque is a different program and must not inherit this target."),
  R("cooling", "Controlled cooling hold", "After stoneware reaches peak temperature, Kiln holds the cooling ramp at 1050 degrees Celsius for twenty minutes to stabilize the glaze surface.", kind="decision"),
  R("symbol", "Program ramp assembler", "buildStonewareRamp in furnace/programs/stoneware.py inserts the twenty-minute cooling hold. Its HOLD_TEMP_C value is 1050.", kind="code-reference"),
  R("loading", "Shelf clearance note", "Stoneware shelves need at least twenty millimeters of clearance above the tallest piece. The clearance instruction concerns placement, not target cone."),
  R("old", "Old cooling hold", "The early stoneware recipe paused at 1100 degrees Celsius for ten minutes.", status="superseded", revision="r1"),
  R("change", "Glaze pinhole adjustment", "The accepted glaze adjustment moved the hold to 1050 degrees Celsius and doubled it to twenty minutes. The target cone remained six.", kind="decision", supersedes=["old"]),
  R("bisque", "Bisque program target", "The bisque task uses cone zero-four with the vent open during the early ramp. These instructions do not define the stoneware program.", task="bisque"),
  R("pref", "Pia's batch log preference", "Pia wants batch logs to record the actual witness-cone bend in words alongside the programmed target. A set point alone is not an observation of the firing.", kind="preference", level=2),
  R("observation", "Glaze tile inspection", "Synthetic test tiles showed fewer pinholes after the adjusted hold. The note supports the specific comparison but does not establish results for every glaze.", kind="observation", epistemic="verified"),
  R("guess", "Fan acceleration idea", "A helper suggested forcing cooling with a portable fan. No approved firing instruction or inspection supports that method.", kind="hypothesis", epistemic="claimed"),
 ], [
  Q("constant", "identifiers", "What value does HOLD_TEMP_C use in the stoneware ramp?", {"symbol":3,"cooling":2,"change":2}, "The symbol record provides the constant and location; both cooling decisions explicitly corroborate its value."),
  Q("paraphrase", "paraphrases", "How long does the pottery controller linger while cooling, and at what temperature?", {"cooling":3,"symbol":2,"change":2}, "The current stoneware hold answers both duration and temperature; its assembler and change decision explicitly corroborate them."),
  Q("history", "supersession", "What changed in the cooling hold to address pinholes?", {"old":2,"change":3,"cooling":2,"symbol":2,"observation":1}, "The decision gives the change; old recipe and current cooling/assembler records establish its endpoints. Tile inspection is limited supporting evidence.", statuses=["current","superseded"]),
  Q("scope", "task-scope", "What cone applies to the bisque program?", {"bisque":3}, "The task rule selects bisque; stoneware cone six is ineligible.", tasks=["bisque"]),
  Q("multi", "multi-record", "State the shelf clearance and what Pia wants observed in the batch log.", {"loading":3,"pref":3}, "Placement clearance and observed witness-cone reporting are independent requirements."),
  Q("absent", "unanswerable", "What approved fan speed should accelerate stoneware cooling?", {}, "Only an unsupported idea mentions a fan, and it specifies neither approval nor speed."),
 ]),
 C("mosaic_accessibility", "Mosaic", "Ana", "color-picker", [
  R("keyboard", "Color grid keyboard motion", "Mosaic's color grid moves one swatch with each arrow key. Home selects the first swatch in the current row; End selects the last."),
  R("focus", "Swatch focus policy", "The color picker uses a single tab stop and roving focus inside the grid. Arrow movement previews a swatch, while Enter commits the color.", kind="decision"),
  R("symbol", "Swatch key handler", "handleSwatchKey in ui/color/swatch_keys.ts distinguishes preview movement from commit. COLOR_COMMIT is dispatched only for Enter or a pointer activation.", kind="code-reference"),
  R("label", "Accessible swatch names", "Each swatch's accessible name combines its human color name and hex value. Selection state is exposed separately, so the name does not grow every time the color changes."),
  R("old", "Former tab-per-swatch interaction", "The original color picker put every swatch into the page tab order.", status="superseded", revision="r1"),
  R("change", "Shorter keyboard traversal", "A single tab stop replaced tab-per-swatch navigation so users could pass the picker without traversing the entire palette.", kind="decision", supersedes=["old"]),
  R("other", "Mosaic tile inventory colors", "The warehouse task uses color names as inventory variants. Keyboard arrow keys there move between stock-count cells, not swatches.", task="warehouse"),
  R("pref", "Ana's picker feedback preference", "Ana wants the preview color shown beside the current committed color. The preview should be labeled so users can cancel without confusing it with a saved change.", kind="preference", level=2),
  R("observation", "Keyboard commit observation", "A synthetic Right-Right-Escape sequence moved the preview twice and left the committed color unchanged. Replacing Escape with Enter committed the preview.", kind="observation", epistemic="verified"),
  R("deleted", "Removed user interview text", None, kind="interview", status="deleted", epistemic="claimed", provenance=[]),
 ], [
  Q("identifier", "identifiers", "Which actions dispatch COLOR_COMMIT?", {"symbol":3,"focus":2,"observation":1}, "The handler explicitly lists the dispatch actions; the focus policy and test sequence corroborate commit semantics."),
  Q("paraphrase", "paraphrases", "Can I browse palette choices with arrows without saving a new color?", {"focus":3,"keyboard":2,"symbol":2,"observation":2}, "Roving focus previews without committing; movement rules, explicit commit actions and the Escape sequence corroborate that interpretation."),
  Q("preference", "preferences", "How should Ana compare an unsaved color with the chosen one?", {"pref":3}, "The preference requires side-by-side preview and committed color with a preview label."),
  Q("history", "historical-revisions", "Why was tabbing through every swatch abandoned?", {"change":3,"old":2}, "The decision supplies the reason and the former interaction identifies what was replaced.", statuses=["current","superseded"]),
  Q("multi", "multi-record", "Describe the swatch accessible name and which key selects the row's last swatch.", {"label":3,"keyboard":3}, "Accessible naming and End-key movement are distinct parts of the question."),
  Q("deleted", "deleted-payload", "What exact complaint did the removed interview participant make?", {}, "The interview payload is deleted, and general design records do not reproduce a participant quote.", statuses=["current","deleted"]),
 ]),
]
